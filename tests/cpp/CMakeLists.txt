# C++ extension test suite
# Consolidates DEW and Perple_X C++ tests so 'ctest' can run them all.
# The main reaktoro-cpptests (Catch2, *.test.cxx) is registered in Reaktoro/CMakeLists.txt.

add_subdirectory(perplex)
